import React, { Component } from 'react';

import '../scss/AmazonPrimeButton.scss'

export default class AmazonPrimeButton extends Component {
    render() {
        return (
            <div className="AmazonPrimeButton">
                
            </div>
        );
    }
}